procs:
  api-gateway:
    shell: "cd api-gateway && bun install && bun dev"
    autoRestart: true

  auth-service:
    shell: "cd services/auth-service && bun install && bun dev"
    autoRestart: true
    
  email-service:
    shell: "cd services/email-service && bun install && bun dev"
    autoRestart: true

  outlet-web:
    shell: "cd clients/outlet-web && bun install && bun dev"
    autoRestart: true

  admin-portal:
    shell: "cd clients/admin-portal && bun install && bun dev"
    autoRestart: true

  company-portal:
    shell: "cd clients/company-portal && bun install && bun dev"
    autoRestart: true
